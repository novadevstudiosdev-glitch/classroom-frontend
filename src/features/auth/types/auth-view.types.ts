export type AuthMode = "login" | "register";

export type AuthFormData = {
  first_name: string;
  last_name: string;
  childName: string;
  schoolName: string;
  email: string;
  password: string;
  confirmPassword: string;
};
